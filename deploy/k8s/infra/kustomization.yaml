---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  - helmrepos.yaml
  - flagger.yaml
  - flagger-scrape.yaml
  - flagger-loadtester.yaml
  - nats.yaml
  - valkey.yaml
  - fleet-inputs.yaml
  - cilium-policies.yaml
  # Policies land last so every workload they select already exists in the
  # same apply set (audit mode makes order non-enforcing, but the read
